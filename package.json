{
  "private": true,
  "scripts": {
    "start": "maizzle serve",
    "build:local": "maizzle build",
    "build:production": "maizzle build production",
    "clean": "rm -rf build_local && rm -rf build_production"
  },
  "dependencies": {
    "@maizzle/framework": "^3.0.0"
  }
}
